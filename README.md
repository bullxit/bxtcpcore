# TCP Server & Client Library

A lightweight and easy-to-use .NET library for creating **TCP servers** and **TCP clients** with minimal setup.  
Designed to simplify socket programming while remaining flexible for advanced scenarios.

---

## ✨ Features
- Quickly spin up a TCP server that can handle multiple clients.
- Easily connect as a TCP client to any server.
- Asynchronous API for scalable network applications.
- Minimal boilerplate code — focus on your logic, not socket plumbing.
- Compatible with both legacy and modern .NET platforms.

---

## 🚀 Getting Started

### Install via NuGet
```powershell
dotnet add package bx.network.tcp.core
